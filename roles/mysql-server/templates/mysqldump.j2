# Copyright © 2014 Martin Ueding <dev@martin-ueding.de>
# {{ ansible_managed }}

5 0 * * * mysqldump -u root -p{{ mysql_root_passwd }} --all-databases | gzip > /var/backups/mysql/$(date -u +%FT%T).sql.gz

# vim: ft=crontab
