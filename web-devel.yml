- name: Set up web development
  hosts:
  - localhost
  sudo: yes
  tasks:
  - name: Install packages
    action: apt pkg={{ item }} state=present
    with_items:
    - apache2
    - libapache2-mod-php5
    - php5-mysql
